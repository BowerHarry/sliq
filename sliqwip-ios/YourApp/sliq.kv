<BoardTile>:
    size: 100, 100
    Image:
        source: root.s
        # keep_ratio: False
        # pos_hint: {'x': 0.0, 'y': 0.5}
        # pos: self.x, self.y
        size_hint_x: None
        size_hint_y: None
        size_x: 100
        size_y:100
        # size_hint: 0.125, 0.125 # if you are setting the size, the hint must be set to None
        # size: 100, 100

<TileVerticalEdge>:
    Image:
        source: root.s
        keep_ratio: False
        # pos_hint: {'x': 0.0, 'y': 0.5}
        # pos: root.x, root.y
        size_hint: None, None # if you are setting the size, the hint must be set to None
        size: 20, 100

<TileHorizontalEdge>:
    Image:
        source: root.s
        keep_ratio: False
        # pos_hint: {'x': 0.0, 'y': 0.5}
        # pos: root.x, root.y
        size_hint: None, None # if you are setting the size, the hint must be set to None
        size: 100, 20


<Board>:



<SliqGame>:
    board: board
    score: score

    Button:
        color: (1 , 1 , 1 , 1)
        background_color: (0 , 1 , 0 , 1)
        font_size: 70  
        center_x: (root.width * 0.5)
        top: root.height - (0.8 * root.height)
        text: 'Rotate!'
        on_press: root.rotateBorder()
        size: 250, 70

    Label:
        color: (1 , 0 , 0 , 1)
        font_size: 70  
        id: score
        points: board.score
        center_x: (root.width * 0.5)
        center_y: (root.height * 0.9)
        size: 250, 70
        text: "Score: " + str(self.points)
    
    Board:
        id: board
        center_x: (root.width / 2) -450
        center_y: (root.height / 2) -400
